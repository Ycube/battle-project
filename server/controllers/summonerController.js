//summonerController.js
var Summoner = require('../db/models/summoner')

module.exports = {
  getAllSummoners : function(req, res, next) {
    //TODO
    //display all summoners currently saved into mongoosedb
  },

  saveSummoner : function(req, res, next) {
    //TODO
    //should hit the Summoner model and save it into db
    
    //Summoner.save();
  }
};  